
<h3 class="ui dividing header">課堂筆記-<%= @coursetextbook.title %>(<%= @note.user.name%>)</h3>
<div class="ui clearing segment">
  <div id="note">
    <div class="container" >
      <div class="content" id="notecontent">
        <div class="text" id="notetext" >
          <%= raw @note.content %>
        </div>
        <div class="metadata">
          <span class="date"><%= @note.created_at %></span>
        </div>
        <br>
        <% if current_user.id == @note.user_id %>
        
      <% end %>
      </div>
    </div>
    <%= link_to '我的筆記本', users_path%>｜
    <%= link_to '筆記課程', course_coursetextbook_path(@course,@coursetextbook) %>
  </div>
</div>