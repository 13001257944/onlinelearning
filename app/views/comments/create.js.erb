var comment = document.createElement("div");
comment.id = "comment-<%= @comment.id %>";
comment.innerHTML = "<%= j render(:partial => "shared/comment",  locals: {comment: @comment}) %>";
$("#commentlist").append(comment);